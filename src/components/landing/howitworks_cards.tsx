interface HowItWorksCardsProps {
  icon: string;
  title: string;
  description: string;
}

export default function HowItWorksCards({
  icon,
  title,
  description,
}: HowItWorksCardsProps) {
    return (
        <div>
            
        </div>       
    );
}